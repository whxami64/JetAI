# Copy to .env and fill in. The agents read these at runtime.

# OpenAI API key for the audit agent suite.
OPENAI_API_KEY=

# Optional: override the default model.
# OPENAI_MODEL=gpt-5

# Optional: max agent steps (tool calls + model turns) per specialist invocation.
# RECURSION_LIMIT=100
